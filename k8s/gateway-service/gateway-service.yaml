apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: totoro
spec:
  type: LoadBalancer  # Expose gateway externally as the single entry point
  selector:
    app: gateway-service
  ports:
    - protocol: TCP
      port: 80          # External port (HTTP)
      targetPort: 8080  # Container port
